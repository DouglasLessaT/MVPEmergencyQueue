#!/bin/bash

BASE_DIR="."

mkdir -p $BASE_DIR/{core/{constants,utils,widgets,theme}
# mkdir -p $BASE_DIR/data/{models,repositories,providers}
mkdir -p $BASE_DIR/presentation/{screens,widgets,routes}
mkdir -p $BASE_DIR/config/{environment,dependencies}
mkdir -p $BASE_DIR/assets/{images,fonts}
mkdir -p $BASE_DIR/domain/{entities,usecases}